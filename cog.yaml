build:
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  python_packages:
    - torch==2.0.1
    - transformers==4.30.2
    - datasets>=2.12.0
    - accelerate>=0.20.3
    - wandb>=0.15.4
    - huggingface_hub>=0.16.4
    - requests>=2.31.0
    - wandb>=0.19.8
    - huggingface_hub>=0.29.2

# Training configuration
train: python -m train
# If you also want to support inference, you can add this
# predict: python -m predict
